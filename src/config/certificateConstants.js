// Ukuran canvas A4 Landscape dalam piksel (96 DPI)
// 297mm × 210mm → 1122px × 794px
// Ini adalah ukuran "logical" yang dipakai untuk semua koordinat x, y, width, height
// Komponen akan scale secara visual agar muat di layar

export const A4_WIDTH = 1122;
export const A4_HEIGHT = 794;
export const A4_ASPECT = A4_WIDTH / A4_HEIGHT; // ~1.414
